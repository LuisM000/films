{
  "name": "films",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prepare": "husky install",
    "test": "echo \"Error: no test specified\" && exit 1",
    "precommit": "npm-run-all precommit:backend precommit:frontend",
    "precommit:backend": "cd backend && npm run precommit",
    "precommit:frontend": "cd frontend && npm run precommit"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "8.0.3",
    "npm-run-all": "4.1.5"
  }
}